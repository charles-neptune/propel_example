    if (typeof config.iconDisplayShape !== 'string') {
        iconDisplayShape = sap.m.AvatarShape.Square;

    } else if (config.iconDisplayShape === sap.m.AvatarShape.Circle) {
        iconDisplayShape = sap.m.AvatarShape.Circle;
    }
